<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <script type="module" defer src="https://cdn.jsdelivr.net/gh/imananoosheh/github-contributions-fetch@latest/github_calendar_widget.js"></script>
</head>
<body>
    <section class="Eddybanner">
        <img id="eddyprof" src="/images/profile.jpg" alt="Profile Picture" width="200px" height="150px">
        <h1>Min Seuk Kim's Portfolio</h1>
    </section>
    
    <section class="aboutEddy">
        <h1>About Me</h1>
        <h3>I've transformed my life and can do anything</h3>
        <p id="aboutText"><%= aboutEddy %></p>
        <% if (admin1 && !admin2)  { %>
            <form action="/portfolio/update/minseuk" method="POST" id="editForm" style="display: none;">
                <textarea name="aboutText" rows="10" cols="50"><%= aboutEddy %></textarea>
                <button type="submit">Save</button>
            </form>
            <div class="button-container">
                <button onclick="toggleEdit()">Edit</button>
            </div>
        <% } %>
    </section>
    
    <section class="Eddyprojects">
        <div id="projectList">
        <h1>Projects</h1>
        <h2>Unolingo</h2>
        <img src="/images/Duck_define.png" alt="Project 1" width="150px" height="150px">
        <p>An English learning app that combines fun, interactive lessons with engaging games to make studying english enjoyable and effective. Designed to help users enhance their vocabulary while having fun and staying motivated
           I mostly worked on routing and login features, as well as the backend database to store user information and progress. I also implemented a feature that allows users to track their progress and see their improvement over time. 
        </p>

        <hr>
        
        <h2>PHSA AI Placement Web App</h2>
        <img src="/images/phsa-banner.png" alt="Project 2" width="250px" height="150px">
        <p>A Web Application that allows Admins to match Health Science Students to preceptors and monitor their progress through an AI algorithm program. Designed to streamline the placement process and improve the overall experience for students and preceptors.
           I was responsible for designing the user interface and implementing the front-end features, such as the student and preceptor profiles, the matching algorithm, and the progress tracking system. I also worked on the backend to ensure that the application could handle large amounts of data and provide accurate matches.
        </p>
        <% if (admin1 && !admin2)  { %>   
            <div class="button-container">
                <button id="addProjectButton" onclick="addProject()">Add Project</button>
            </div>
        <% } %>
        </div>
    </section>
    
    <section id="github">
        <h2>GitHub Contributions</h2>
        <div id="calendar-component" username="cloudtemplr" theme-color="#333333" background-color="#fff"></div>
            <div id="github-contributions"></div>
            <script src="https://cdn.jsdelivr.net/npm/github-contributions-widget@latest/dist/github-contributions-widget.min.js"></script>
            <script>
                new GitHubCalendar("#github-contributions", "your-github-username");
            </script>
        <p>Check out my GitHub profile for more contributions: <a href="https://github.com/cloudtemplr" target="_blank">GitHub Profile</a></p>
    </section>
    <script src="script.js"></script>
</body>
<script>
    function toggleEdit() {
        const aboutText = document.getElementById('aboutText');
        const editForm = document.getElementById('editForm');
        if (editForm.style.display === 'none') {
            aboutText.style.display = 'none';
            editForm.style.display = 'block';
        } else {
            aboutText.style.display = 'block';
            editForm.style.display = 'none';
        }
    }

    function addProject() {
        // Prompt the user for title and description
        const title = prompt("Enter the project title:");
        if (!title) {
            alert("Title cannot be empty!");
            return;
        }

        const description = prompt("Enter the project description:");
        if (!description) {
            alert("Description cannot be empty!");
            return;
        }

        // Get the project list container
        const projectList = document.getElementById('projectList');

        // Create new h3 and p elements
        const newTitle = document.createElement('h3');
        newTitle.textContent = title;

        const newDescription = document.createElement('p');
        newDescription.textContent = description;

        // Append the new elements to the project list
        projectList.appendChild(newTitle);
        projectList.appendChild(newDescription);
    }
</script>
</html>
